"use client";

export default function WorkspacesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Your workspaces</h1>
      <p className="mt-2 text-sm text-[color:var(--muted)]">
        Workspace listing will go here in Phase 2.
      </p>
    </div>
  );
}
